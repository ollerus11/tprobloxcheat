-- XENO MENU - ТЕЛЕПОРТ + ИГРОК + ESP + МЕТКИ + КАСТОМИЗАЦИЯ
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")

-- === ПЕРЕМЕННЫЕ С ЗАГРУЗКОЙ ИЗ _G ===
local FlySpeed = _G.XenoFlySpeed or 50
local FlyEnabled = false
local FlyMode = _G.XenoFlyMode or "Свободный"
local FlyBV = nil
local FlyBG = nil
local FlyConn = nil
local MenuColor = _G.XenoMenuColor or Color3.fromRGB(28, 28, 33)
local MenuTransparency = _G.XenoMenuTransparency or 0.05
local AccentColor = _G.XenoAccentColor or Color3.fromRGB(100, 100, 130)
local Gravity = 196.2
local WalkSpeed = _G.XenoWalkSpeed or 16
local JumpPower = _G.XenoJumpPower or 50
local PlayerGravity = _G.XenoPlayerGravity or 196.2
local Marks = _G.XenoMarks or {}
local SavedBackgroundID = _G.XenoBackgroundID or nil
local GalleryImages = {}
local ESPColor = _G.XenoESPColor or Color3.fromRGB(255, 0, 0)
local ESPTeamColor = _G.XenoESPTeamColor or false
local TracerEnabled = _G.XenoTracerEnabled or false
local BoxEnabled = _G.XenoBoxEnabled or false
local NametagsEnabled = _G.XenoNametagsEnabled or false
local SpinEnabled = _G.XenoSpinEnabled or false
local SpinSpeed = _G.XenoSpinSpeed or 10
local ESPDistance = _G.XenoESPDistance or 500
local ESPRainbow = _G.XenoESPRainbow or false
local ESPRainbowConn = nil
local NoClipEnabled = _G.XenoNoClipEnabled or false
local NoClipConn = nil
local BGRainbow = _G.XenoBGRainbow or false
local BGRainbowConn = nil
local AccentRainbow = _G.XenoAccentRainbow or false
local AccentRainbowConn = nil

-- === ФУНКЦИЯ СОХРАНЕНИЯ ===
local function SaveSettings()
    _G.XenoFlySpeed = FlySpeed
    _G.XenoFlyMode = FlyMode
    _G.XenoMenuColor = MenuColor
    _G.XenoMenuTransparency = MenuTransparency
    _G.XenoAccentColor = AccentColor
    _G.XenoWalkSpeed = WalkSpeed
    _G.XenoJumpPower = JumpPower
    _G.XenoPlayerGravity = PlayerGravity
    _G.XenoMarks = Marks
    _G.XenoBackgroundID = SavedBackgroundID
    _G.XenoESPColor = ESPColor
    _G.XenoESPTeamColor = ESPTeamColor
    _G.XenoTracerEnabled = TracerEnabled
    _G.XenoBoxEnabled = BoxEnabled
    _G.XenoNametagsEnabled = NametagsEnabled
    _G.XenoSpinEnabled = SpinEnabled
    _G.XenoSpinSpeed = SpinSpeed
    _G.XenoESPDistance = ESPDistance
    _G.XenoESPRainbow = ESPRainbow
    _G.XenoNoClipEnabled = NoClipEnabled
    _G.XenoBGRainbow = BGRainbow
    _G.XenoAccentRainbow = AccentRainbow
end

-- === ESP ===
local ESPHighlights = {}
local ESPBeams = {}
local ESPConnection = nil

local function ClearESP()
    for _, obj in pairs(ESPHighlights) do
        if obj then pcall(function() obj:Destroy() end) end
    end
    for _, beam in pairs(ESPBeams) do
        if beam then pcall(function() beam:Destroy() end) end
    end
    ESPHighlights = {}
    ESPBeams = {}
end

local function UpdateESP()
    ClearESP()
    if ESPConnection then ESPConnection:Disconnect() ESPConnection = nil end
    if not TracerEnabled and not BoxEnabled and not NametagsEnabled then return end
    
    ESPConnection = RunService.RenderStepped:Connect(function()
        ClearESP()
        local localRoot = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        local localHead = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Head")
        if not localRoot then return end
        
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                local root = player.Character:FindFirstChild("HumanoidRootPart")
                local head = player.Character:FindFirstChild("Head")
                if root then
                    local distance = (localRoot.Position - root.Position).Magnitude
                    if distance <= ESPDistance then
                        local espColor
                        if ESPTeamColor then
                            if player.Team then espColor = player.Team.TeamColor.Color
                            elseif player.TeamColor then espColor = player.TeamColor.Color
                            else espColor = Color3.fromRGB(170, 170, 180) end
                        else
                            espColor = ESPColor
                        end
                        
                        if TracerEnabled and localHead then
                            local beam = Instance.new("Beam")
                            beam.Parent = workspace
                            local attach0 = Instance.new("Attachment")
                            attach0.Parent = localHead
                            local attach1 = Instance.new("Attachment")
                            attach1.Parent = root
                            beam.Attachment0 = attach0
                            beam.Attachment1 = attach1
                            beam.Color = ColorSequence.new(espColor)
                            beam.Width0 = 0.2
                            beam.Width1 = 0.2
                            beam.Transparency = NumberSequence.new(0)
                            beam.Enabled = true
                            table.insert(ESPBeams, beam)
                            table.insert(ESPBeams, attach0)
                            table.insert(ESPBeams, attach1)
                        end
                        
                        if BoxEnabled then
                            local highlight = Instance.new("Highlight")
                            highlight.Parent = player.Character
                            highlight.FillColor = espColor
                            highlight.OutlineColor = espColor
                            highlight.FillTransparency = 0.7
                            highlight.OutlineTransparency = 0
                            highlight.Adornee = player.Character
                            highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                            table.insert(ESPHighlights, highlight)
                        end
                        
                        if NametagsEnabled and head then
                            local billboard = Instance.new("BillboardGui")
                            billboard.Parent = head
                            billboard.Size = UDim2.new(0, 200, 0, 40)
                            billboard.StudsOffset = Vector3.new(0, 3, 0)
                            billboard.AlwaysOnTop = true
                            billboard.MaxDistance = ESPDistance
                            billboard.Adornee = head
                            local label = Instance.new("TextLabel")
                            label.Parent = billboard
                            label.Size = UDim2.new(1, 0, 1, 0)
                            label.BackgroundColor3 = Color3.new(0, 0, 0)
                            label.BackgroundTransparency = 0.3
                            label.Text = player.Name
                            label.TextColor3 = espColor
                            label.Font = Enum.Font.GothamBold
                            label.TextSize = 14
                            label.TextStrokeTransparency = 0
                            label.TextStrokeColor3 = Color3.new(0, 0, 0)
                            table.insert(ESPHighlights, billboard)
                        end
                    end
                end
            end
        end
    end)
end

local function SetESPRainbow(enabled)
    ESPRainbow = enabled
    _G.XenoESPRainbow = enabled
    if ESPRainbowConn then ESPRainbowConn:Disconnect() ESPRainbowConn = nil end
    if enabled then
        ESPTeamColor = false
        _G.XenoESPTeamColor = false
        local hue = 0
        ESPRainbowConn = RunService.RenderStepped:Connect(function(dt)
            hue = (hue + dt * 0.5) % 1
            ESPColor = HSVToColor(hue, 1, 1)
            _G.XenoESPColor = ESPColor
        end)
    end
end

-- === ФУНКЦИИ ПОЛЕТА ===
local function StartFly()
    local char = LocalPlayer.Character
    if not char then return end
    local hum = char:FindFirstChild("Humanoid")
    local root = char:FindFirstChild("HumanoidRootPart")
    if not hum or not root then return end
    
    hum.PlatformStand = true
    workspace.Gravity = 0
    
    local bv = Instance.new("BodyVelocity")
    bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
    bv.Velocity = Vector3.new(0, 0, 0)
    bv.Parent = root
    
    local bg = Instance.new("BodyGyro")
    bg.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
    bg.CFrame = root.CFrame
    bg.Parent = root
    
    FlyBV = bv
    FlyBG = bg
    FlyEnabled = true
    
    FlyConn = RunService.Heartbeat:Connect(function()
        if not FlyEnabled then return end
        local rootPart = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if not rootPart then return end
        
        local move = Vector3.new(0, 0, 0)
        local cam = workspace.CurrentCamera
        
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then move = move + cam.CFrame.LookVector * Vector3.new(1, 0, 1) end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then move = move - cam.CFrame.LookVector * Vector3.new(1, 0, 1) end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then move = move - cam.CFrame.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then move = move + cam.CFrame.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then move = move + Vector3.new(0, 1, 0) end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then move = move - Vector3.new(0, 1, 0) end
        
        if move.Magnitude > 0 then move = move.Unit * FlySpeed end
        
        if FlyMode == "Свободный" then
            FlyBV.Velocity = move
            FlyBG.CFrame = cam.CFrame
        elseif FlyMode == "По курсору" then
            local mouse = LocalPlayer:GetMouse()
            if mouse then
                local target = mouse.Hit.Position
                local dir = (target - rootPart.Position).Unit
                FlyBV.Velocity = dir * FlySpeed
                FlyBG.CFrame = CFrame.lookAt(rootPart.Position, target)
            end
        elseif FlyMode == "По взгляду" then
            FlyBV.Velocity = cam.CFrame.LookVector * FlySpeed + move
            FlyBG.CFrame = cam.CFrame
        end
    end)
end

local function StopFly()
    FlyEnabled = false
    if FlyConn then FlyConn:Disconnect() FlyConn = nil end
    if FlyBV then FlyBV:Destroy() FlyBV = nil end
    if FlyBG then FlyBG:Destroy() FlyBG = nil end
    
    local char = LocalPlayer.Character
    if char then
        local hum = char:FindFirstChild("Humanoid")
        if hum then hum.PlatformStand = false end
    end
    workspace.Gravity = PlayerGravity
end

local function ApplyCharacteristics()
    local char = LocalPlayer.Character
    if not char then return end
    local hum = char:FindFirstChild("Humanoid")
    if hum then
        hum.WalkSpeed = WalkSpeed
        hum.JumpPower = JumpPower
    end
    workspace.Gravity = PlayerGravity
    SaveSettings()
end

-- Крутилка
local SpinConn = nil
local function StartSpin()
    if SpinConn then SpinConn:Disconnect() end
    SpinConn = RunService.Heartbeat:Connect(function()
        local char = LocalPlayer.Character
        if char and char:FindFirstChild("HumanoidRootPart") then
            char.HumanoidRootPart.CFrame = char.HumanoidRootPart.CFrame * CFrame.Angles(0, math.rad(SpinSpeed), 0)
        end
    end)
    SpinEnabled = true
    _G.XenoSpinEnabled = true
end

local function StopSpin()
    if SpinConn then SpinConn:Disconnect() SpinConn = nil end
    SpinEnabled = false
    _G.XenoSpinEnabled = false
end

-- NoClip
local function StartNoClip()
    if NoClipConn then NoClipConn:Disconnect() end
    NoClipConn = RunService.Stepped:Connect(function()
        if NoClipEnabled and LocalPlayer.Character then
            for _, part in pairs(LocalPlayer.Character:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = false
                end
            end
        end
    end)
    NoClipEnabled = true
    _G.XenoNoClipEnabled = true
end

local function StopNoClip()
    if NoClipConn then NoClipConn:Disconnect() NoClipConn = nil end
    NoClipEnabled = false
    _G.XenoNoClipEnabled = false
    if LocalPlayer.Character then
        for _, part in pairs(LocalPlayer.Character:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = true
            end
        end
    end
end

local function GetPlayerTeam(player)
    if player.Team then return player.Team.Name
    elseif player.TeamColor and player.TeamColor ~= BrickColor.new("Medium stone grey") then return tostring(player.TeamColor)
    else return "Нет команды" end
end

local function GetTeamColor(player)
    if player.Team then return player.Team.TeamColor.Color
    elseif player.TeamColor then return player.TeamColor.Color
    else return Color3.fromRGB(170, 170, 180) end
end

local function ApplyCorner(element, radius)
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, radius or 8)
    corner.Parent = element
end

local function HSVToColor(h, s, v)
    local r, g, b
    local i = math.floor(h * 6)
    local f = h * 6 - i
    local p = v * (1 - s)
    local q = v * (1 - f * s)
    local t = v * (1 - (1 - f) * s)
    i = i % 6
    if i == 0 then r, g, b = v, t, p
    elseif i == 1 then r, g, b = q, v, p
    elseif i == 2 then r, g, b = p, v, t
    elseif i == 3 then r, g, b = p, q, v
    elseif i == 4 then r, g, b = t, p, v
    else r, g, b = v, p, q end
    return Color3.new(r, g, b)
end

local function ColorToHSV(color)
    local r, g, b = color.R, color.G, color.B
    local max = math.max(r, g, b)
    local min = math.min(r, g, b)
    local h, s, v = 0, 0, max
    local d = max - min
    if max == 0 then s = 0 else s = d / max end
    if max == min then h = 0
    elseif max == r then h = (g - b) / d + (g < b and 6 or 0)
    elseif max == g then h = (b - r) / d + 2
    else h = (r - g) / d + 4 end
    h = h / 6
    return h, s, v
end

local function CreateColorPalette(parent, label, defaultColor, callback, yPos)
    local frame = Instance.new("Frame")
    frame.Parent = parent
    frame.BackgroundColor3 = Color3.fromRGB(35, 35, 40)
    frame.BackgroundTransparency = 0.3
    frame.BorderSizePixel = 1
    frame.BorderColor3 = Color3.fromRGB(70, 70, 80)
    frame.Size = UDim2.new(1, -20, 0, 200)
    frame.Position = UDim2.new(0, 10, 0, yPos or 0)
    frame.ZIndex = 1002
    ApplyCorner(frame, 8)
    
    local labelText = Instance.new("TextLabel")
    labelText.Parent = frame
    labelText.BackgroundTransparency = 1
    labelText.Size = UDim2.new(1, -20, 0, 20)
    labelText.Position = UDim2.new(0, 10, 0, 5)
    labelText.Text = label
    labelText.TextColor3 = Color3.fromRGB(235, 235, 240)
    labelText.TextSize = 13
    labelText.Font = Enum.Font.GothamBold
    labelText.ZIndex = 1003
    
    local palette = Instance.new("ImageLabel")
    palette.Parent = frame
    palette.BackgroundColor3 = Color3.new(1, 1, 1)
    palette.BorderSizePixel = 0
    palette.Size = UDim2.new(1, -20, 0, 120)
    palette.Position = UDim2.new(0, 10, 0, 30)
    palette.ZIndex = 1003
    ApplyCorner(palette, 6)
    palette.ClipsDescendants = true
    
    local hueGradient = Instance.new("UIGradient")
    hueGradient.Parent = palette
    hueGradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 0, 0)),
        ColorSequenceKeypoint.new(0.17, Color3.fromRGB(255, 128, 0)),
        ColorSequenceKeypoint.new(0.33, Color3.fromRGB(255, 255, 0)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0, 255, 0)),
        ColorSequenceKeypoint.new(0.67, Color3.fromRGB(0, 255, 255)),
        ColorSequenceKeypoint.new(0.83, Color3.fromRGB(0, 0, 255)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 0, 255))
    })
    
    local whiteGradient = Instance.new("UIGradient")
    whiteGradient.Parent = palette
    whiteGradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.new(1, 1, 1)),
        ColorSequenceKeypoint.new(1, Color3.new(1, 1, 1))
    })
    whiteGradient.Rotation = 90
    whiteGradient.Transparency = NumberSequence.new({
        NumberSequenceKeypoint.new(0, 1),
        NumberSequenceKeypoint.new(1, 0)
    })
    
    local cursor = Instance.new("Frame")
    cursor.Parent = palette
    cursor.BackgroundColor3 = Color3.new(1, 1, 1)
    cursor.BorderSizePixel = 2
    cursor.BorderColor3 = Color3.new(0, 0, 0)
    cursor.Size = UDim2.new(0, 16, 0, 16)
    cursor.Position = UDim2.new(0.5, -8, 0.5, -8)
    cursor.ZIndex = 1005
    ApplyCorner(cursor, 8)
    cursor.BackgroundTransparency = 0.3
    
    local brightnessLabel = Instance.new("TextLabel")
    brightnessLabel.Parent = frame
    brightnessLabel.BackgroundTransparency = 1
    brightnessLabel.Size = UDim2.new(0, 50, 0, 20)
    brightnessLabel.Position = UDim2.new(0, 10, 0, 155)
    brightnessLabel.Text = "Яркость:"
    brightnessLabel.TextColor3 = Color3.fromRGB(235, 235, 240)
    brightnessLabel.TextSize = 11
    brightnessLabel.Font = Enum.Font.Gotham
    brightnessLabel.ZIndex = 1003
    
    local brightnessBg = Instance.new("Frame")
    brightnessBg.Parent = frame
    brightnessBg.BackgroundColor3 = Color3.fromRGB(50, 50, 55)
    brightnessBg.BorderSizePixel = 0
    brightnessBg.Size = UDim2.new(1, -70, 0, 16)
    brightnessBg.Position = UDim2.new(0, 60, 0, 157)
    brightnessBg.ZIndex = 1003
    ApplyCorner(brightnessBg, 8)
    
    local brightnessGradient = Instance.new("UIGradient")
    brightnessGradient.Parent = brightnessBg
    brightnessGradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.new(0, 0, 0)),
        ColorSequenceKeypoint.new(1, Color3.new(1, 1, 1))
    })
    
    local brightnessButton = Instance.new("TextButton")
    brightnessButton.Parent = brightnessBg
    brightnessButton.BackgroundColor3 = Color3.fromRGB(235, 235, 240)
    brightnessButton.BorderSizePixel = 2
    brightnessButton.BorderColor3 = Color3.fromRGB(100, 100, 130)
    brightnessButton.Size = UDim2.new(0, 20, 0, 20)
    brightnessButton.Position = UDim2.new(1, -10, 0.5, -10)
    brightnessButton.Text = ""
    brightnessButton.ZIndex = 1005
    brightnessButton.AutoButtonColor = false
    ApplyCorner(brightnessButton, 10)
    
    local draggingPalette = false
    local draggingBrightness = false
    local currentHue = 0
    local currentSaturation = 0
    local currentValue = 1
    
    local initH, initS, initV = ColorToHSV(defaultColor)
    currentHue = initH
    currentSaturation = initS
    currentValue = initV
    
    local function UpdateColor()
        local color = HSVToColor(currentHue, currentSaturation, currentValue)
        cursor.BackgroundColor3 = color
        brightnessButton.Position = UDim2.new(currentValue, -10, 0.5, -10)
        callback(color)
    end
    
    local function GetColorFromPalette(relX, relY)
        currentHue = relX
        currentSaturation = relY
        cursor.Position = UDim2.new(relX, -8, relY, -8)
        UpdateColor()
    end
    
    palette.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            draggingPalette = true
            local relX = math.clamp((input.Position.X - palette.AbsolutePosition.X) / palette.AbsoluteSize.X, 0, 1)
            local relY = math.clamp((input.Position.Y - palette.AbsolutePosition.Y) / palette.AbsoluteSize.Y, 0, 1)
            GetColorFromPalette(relX, relY)
        end
    end)
    
    UserInputService.InputChanged:Connect(function(input)
        if draggingPalette and input.UserInputType == Enum.UserInputType.MouseMovement then
            local relX = math.clamp((input.Position.X - palette.AbsolutePosition.X) / palette.AbsoluteSize.X, 0, 1)
            local relY = math.clamp((input.Position.Y - palette.AbsolutePosition.Y) / palette.AbsoluteSize.Y, 0, 1)
            GetColorFromPalette(relX, relY)
        end
    end)
    
    local function UpdateBrightness(mouseX)
        local relX = math.clamp((mouseX - brightnessBg.AbsolutePosition.X) / brightnessBg.AbsoluteSize.X, 0, 1)
        currentValue = relX
        UpdateColor()
    end
    
    brightnessBg.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            draggingBrightness = true
            UpdateBrightness(input.Position.X)
        end
    end)
    
    brightnessButton.MouseButton1Down:Connect(function()
        draggingBrightness = true
    end)
    
    UserInputService.InputChanged:Connect(function(input)
        if draggingBrightness and input.UserInputType == Enum.UserInputType.MouseMovement then
            UpdateBrightness(input.Position.X)
        end
    end)
    
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            draggingPalette = false
            draggingBrightness = false
        end
    end)
    
    cursor.Position = UDim2.new(currentHue, -8, currentSaturation, -8)
    brightnessButton.Position = UDim2.new(currentValue, -10, 0.5, -10)
    UpdateColor()
    
    return frame
end

-- === СОЗДАНИЕ GUI ===
local function CreateMenu()
    if _G.MenuGUI then _G.MenuGUI:Destroy() _G.MenuGUI = nil end
    
    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = "XenoMenu"
    ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
    ScreenGui.ResetOnSpawn = false
    _G.MenuGUI = ScreenGui
    
    local Colors = {
        BG = MenuColor,
        BG2 = Color3.fromRGB(38, 38, 43),
        BG3 = Color3.fromRGB(48, 48, 53),
        Border = Color3.fromRGB(70, 70, 80),
        Accent = AccentColor,
        Accent2 = Color3.fromRGB(130, 130, 170),
        Text = Color3.fromRGB(235, 235, 240),
        TextDim = Color3.fromRGB(170, 170, 180),
        Green = Color3.fromRGB(0, 200, 100),
        Red = Color3.fromRGB(200, 60, 60),
        Yellow = Color3.fromRGB(200, 200, 60)
    }
    
    local MainFrame = Instance.new("Frame")
    MainFrame.Parent = ScreenGui
    MainFrame.BackgroundColor3 = Colors.BG
    MainFrame.BackgroundTransparency = MenuTransparency
    MainFrame.BorderSizePixel = 0
    MainFrame.Size = UDim2.new(0, 500, 0, 540)
    MainFrame.Position = UDim2.new(0.5, -250, 0.5, -270)
    MainFrame.Active = true
    MainFrame.ClipsDescendants = true
    MainFrame.ZIndex = 999
    ApplyCorner(MainFrame, 12)
    
    if SavedBackgroundID then
        local savedImage = Instance.new("ImageLabel")
        savedImage.Parent = MainFrame
        savedImage.BackgroundTransparency = 1
        savedImage.Size = UDim2.new(1, 0, 1, -40)
        savedImage.Position = UDim2.new(0, 0, 0, 40)
        savedImage.Image = SavedBackgroundID
        savedImage.ZIndex = 998
        savedImage.ScaleType = Enum.ScaleType.Crop
        ApplyCorner(savedImage, 12)
    end
    
    local Header = Instance.new("Frame")
    Header.Parent = MainFrame
    Header.BackgroundColor3 = Colors.Accent
    Header.BackgroundTransparency = 0.3
    Header.BorderSizePixel = 0
    Header.Size = UDim2.new(1, 0, 0, 40)
    Header.ZIndex = 1000
    ApplyCorner(Header, 12)
    
    local dragging = false
    local dragOffset = Vector2.new(0, 0)
    
    Header.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            local mousePos = UserInputService:GetMouseLocation()
            dragOffset = Vector2.new(mousePos.X - MainFrame.AbsolutePosition.X, mousePos.Y - MainFrame.AbsolutePosition.Y)
        end
    end)
    
    UserInputService.InputChanged:Connect(function(input)
        if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
            local mousePos = UserInputService:GetMouseLocation()
            MainFrame.Position = UDim2.new(0, mousePos.X - dragOffset.X, 0, mousePos.Y - dragOffset.Y)
        end
    end)
    
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end
    end)
    
    local Title = Instance.new("TextLabel")
    Title.Parent = Header
    Title.BackgroundTransparency = 1
    Title.Size = UDim2.new(1, -50, 1, 0)
    Title.Position = UDim2.new(0, 15, 0, 0)
    Title.Text = "XENO MENU"
    Title.TextColor3 = Colors.Text
    Title.TextSize = 17
    Title.TextXAlignment = Enum.TextXAlignment.Left
    Title.Font = Enum.Font.GothamBold
    Title.ZIndex = 1001
    
    local CloseBtn = Instance.new("TextButton")
    CloseBtn.Parent = Header
    CloseBtn.BackgroundColor3 = Colors.Red
    CloseBtn.BackgroundTransparency = 0.3
    CloseBtn.BorderSizePixel = 0
    CloseBtn.Size = UDim2.new(0, 28, 0, 28)
    CloseBtn.Position = UDim2.new(1, -36, 0, 6)
    CloseBtn.Text = "✕"
    CloseBtn.TextColor3 = Colors.Text
    CloseBtn.TextSize = 16
    CloseBtn.Font = Enum.Font.GothamBold
    CloseBtn.ZIndex = 1001
    ApplyCorner(CloseBtn, 14)
    CloseBtn.MouseButton1Click:Connect(function()
        SaveSettings()
        if FlyEnabled then StopFly() end
        if SpinEnabled then StopSpin() end
        if NoClipEnabled then StopNoClip() end
        ClearESP()
        ScreenGui:Destroy()
        _G.MenuGUI = nil
    end)
    
    -- ИЗМЕНЕНИЕ РАЗМЕРА ОКНА
    local resizing = false
    local resizeEdge = nil
    local minWidth = 400
    local maxWidth = 1000
    local minHeight = 400
    local maxHeight = 800
    
    local resizeHandle = Instance.new("TextButton")
    resizeHandle.Parent = MainFrame
    resizeHandle.BackgroundColor3 = Colors.Accent
    resizeHandle.BackgroundTransparency = 0.5
    resizeHandle.BorderSizePixel = 0
    resizeHandle.Size = UDim2.new(0, 20, 0, 20)
    resizeHandle.Position = UDim2.new(1, -20, 1, -20)
    resizeHandle.Text = "◢"
    resizeHandle.TextColor3 = Colors.Text
    resizeHandle.TextSize = 12
    resizeHandle.Font = Enum.Font.GothamBold
    resizeHandle.ZIndex = 1002
    resizeHandle.AutoButtonColor = false
    ApplyCorner(resizeHandle, 4)
    
    local rightEdge = Instance.new("TextButton")
    rightEdge.Parent = MainFrame
    rightEdge.BackgroundTransparency = 1
    rightEdge.BorderSizePixel = 0
    rightEdge.Size = UDim2.new(0, 5, 1, -20)
    rightEdge.Position = UDim2.new(1, -5, 0, 0)
    rightEdge.Text = ""
    rightEdge.ZIndex = 1001
    rightEdge.AutoButtonColor = false
    
    local bottomEdge = Instance.new("TextButton")
    bottomEdge.Parent = MainFrame
    bottomEdge.BackgroundTransparency = 1
    bottomEdge.BorderSizePixel = 0
    bottomEdge.Size = UDim2.new(1, -20, 0, 5)
    bottomEdge.Position = UDim2.new(0, 0, 1, -5)
    bottomEdge.Text = ""
    bottomEdge.ZIndex = 1001
    bottomEdge.AutoButtonColor = false
    
    local function StartResize(edge)
        resizing = true
        resizeEdge = edge
    end
    
    resizeHandle.MouseButton1Down:Connect(function() StartResize("corner") end)
    rightEdge.MouseButton1Down:Connect(function() StartResize("right") end)
    bottomEdge.MouseButton1Down:Connect(function() StartResize("bottom") end)
    
    UserInputService.InputChanged:Connect(function(input)
        if resizing and input.UserInputType == Enum.UserInputType.MouseMovement then
            local mousePos = UserInputService:GetMouseLocation()
            local framePos = MainFrame.AbsolutePosition
            local frameSize = MainFrame.AbsoluteSize
            
            if resizeEdge == "corner" or resizeEdge == "right" then
                local newWidth = mousePos.X - framePos.X
                newWidth = math.clamp(newWidth, minWidth, maxWidth)
                MainFrame.Size = UDim2.new(0, newWidth, 0, frameSize.Y)
            end
            
            if resizeEdge == "corner" or resizeEdge == "bottom" then
                local newHeight = mousePos.Y - framePos.Y
                newHeight = math.clamp(newHeight, minHeight, maxHeight)
                MainFrame.Size = UDim2.new(0, MainFrame.AbsoluteSize.X, 0, newHeight)
            end
        end
    end)
    
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            resizing = false
            resizeEdge = nil
        end
    end)
    
    local TabContainer = Instance.new("Frame")
    TabContainer.Parent = MainFrame
    TabContainer.BackgroundColor3 = Colors.BG2
    TabContainer.BackgroundTransparency = 0.5
    TabContainer.BorderSizePixel = 0
    TabContainer.Size = UDim2.new(0, 120, 1, -40)
    TabContainer.Position = UDim2.new(0, 0, 0, 40)
    TabContainer.ZIndex = 1000
    
    local tabs = {}
    local tabNames = {"Телепорт", "Игрок", "ESP", "Метки", "Кастомизация"}
    
    for i, name in ipairs(tabNames) do
        local btn = Instance.new("TextButton")
        btn.Parent = TabContainer
        btn.BackgroundColor3 = (i == 1) and Colors.Accent or Colors.BG3
        btn.BackgroundTransparency = (i == 1) and 0.1 or 0.2
        btn.BorderSizePixel = 0
        btn.Size = UDim2.new(1, -10, 0, 35)
        btn.Position = UDim2.new(0, 5, 0, 10 + (i - 1) * 45)
        btn.Text = name
        btn.TextColor3 = (i == 1) and Colors.Text or Colors.TextDim
        btn.TextSize = 12
        btn.Font = Enum.Font.GothamBold
        btn.ZIndex = 1001
        ApplyCorner(btn, 6)
        tabs[i] = btn
    end
    
    local containers = {}
    
    for i = 1, 5 do
        local frame = Instance.new("ScrollingFrame")
        frame.Parent = MainFrame
        frame.BackgroundTransparency = 1
        frame.BorderSizePixel = 0
        frame.Size = UDim2.new(1, -120, 1, -80)
        frame.Position = UDim2.new(0, 120, 0, 80)
        frame.CanvasSize = UDim2.new(0, 0, 0, 0)
        frame.ScrollBarThickness = 4
        frame.ScrollBarImageColor3 = Colors.Border
        frame.ZIndex = 1000
        frame.Visible = (i == 1)
        
        local container = Instance.new("Frame")
        container.Parent = frame
        container.BackgroundTransparency = 1
        container.Size = UDim2.new(1, 0, 0, 0)
        container.ZIndex = 1001
        
        containers[i] = {frame = frame, container = container}
    end
    
    local function MakeSlider(parent, label, min, max, default, callback)
        local frame = Instance.new("Frame")
        frame.Parent = parent
        frame.BackgroundColor3 = Colors.BG2
        frame.BackgroundTransparency = 0.3
        frame.BorderSizePixel = 1
        frame.BorderColor3 = Colors.Border
        frame.Size = UDim2.new(1, -20, 0, 65)
        frame.Position = UDim2.new(0, 10, 0, 0)
        frame.ZIndex = 1002
        ApplyCorner(frame, 8)
        
        local labelText = Instance.new("TextLabel")
        labelText.Parent = frame
        labelText.BackgroundTransparency = 1
        labelText.Size = UDim2.new(0.6, 0, 0, 24)
        labelText.Position = UDim2.new(0, 12, 0, 6)
        labelText.Text = label
        labelText.TextColor3 = Colors.Text
        labelText.TextSize = 14
        labelText.TextXAlignment = Enum.TextXAlignment.Left
        labelText.Font = Enum.Font.Gotham
        labelText.ZIndex = 1003
        
        local valueText = Instance.new("TextLabel")
        valueText.Parent = frame
        valueText.BackgroundTransparency = 1
        valueText.Size = UDim2.new(0.3, 0, 0, 24)
        valueText.Position = UDim2.new(0.7, 0, 0, 6)
        valueText.Text = tostring(default)
        valueText.TextColor3 = Colors.Accent2
        valueText.TextSize = 15
        valueText.TextXAlignment = Enum.TextXAlignment.Right
        valueText.Font = Enum.Font.GothamBold
        valueText.ZIndex = 1003
        
        local bg = Instance.new("Frame")
        bg.Parent = frame
        bg.BackgroundColor3 = Colors.BG3
        bg.BorderSizePixel = 0
        bg.Size = UDim2.new(1, -24, 0, 8)
        bg.Position = UDim2.new(0, 12, 0, 38)
        bg.ZIndex = 1003
        ApplyCorner(bg, 4)
        
        local fill = Instance.new("Frame")
        fill.Parent = bg
        fill.BackgroundColor3 = Colors.Accent2
        fill.BorderSizePixel = 0
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.ZIndex = 1004
        ApplyCorner(fill, 4)
        
        local button = Instance.new("TextButton")
        button.Parent = bg
        button.BackgroundColor3 = Colors.Text
        button.BorderSizePixel = 2
        button.BorderColor3 = Colors.Accent
        button.Size = UDim2.new(0, 18, 0, 18)
        button.Position = UDim2.new((default - min) / (max - min), -9, 0.5, -9)
        button.Text = ""
        button.ZIndex = 1005
        button.AutoButtonColor = false
        ApplyCorner(button, 9)
        
        local dragging = false
        local function UpdateSlider(mouseX)
            local relX = math.clamp((mouseX - bg.AbsolutePosition.X) / bg.AbsoluteSize.X, 0, 1)
            local value = min + (max - min) * relX
            value = math.floor(value * 10 + 0.5) / 10
            value = math.clamp(value, min, max)
            fill.Size = UDim2.new((value - min) / (max - min), 0, 1, 0)
            button.Position = UDim2.new((value - min) / (max - min), -9, 0.5, -9)
            valueText.Text = tostring(value)
            callback(value)
        end
        
        button.MouseButton1Down:Connect(function() dragging = true end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
                UpdateSlider(input.Position.X)
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end
        end)
        bg.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 then
                UpdateSlider(input.Position.X)
                dragging = true
            end
        end)
        
        return frame
    end
    
    local function MakeButton(parent, text, callback, color)
        local btn = Instance.new("TextButton")
        btn.Parent = parent
        btn.BackgroundColor3 = color or Colors.BG3
        btn.BackgroundTransparency = 0.2
        btn.BorderSizePixel = 1
        btn.BorderColor3 = Colors.Border
        btn.Size = UDim2.new(1, -20, 0, 40)
        btn.Position = UDim2.new(0, 10, 0, 0)
        btn.Text = text
        btn.TextColor3 = Colors.Text
        btn.TextSize = 14
        btn.Font = Enum.Font.Gotham
        btn.ZIndex = 1002
        ApplyCorner(btn, 8)
        btn.MouseButton1Click:Connect(callback)
        return btn
    end
    
    local function CreateAndroidToggle(parent, label, callback, default)
        local frame = Instance.new("Frame")
        frame.Parent = parent
        frame.BackgroundColor3 = Colors.BG2
        frame.BackgroundTransparency = 0.3
        frame.BorderSizePixel = 1
        frame.BorderColor3 = Colors.Border
        frame.Size = UDim2.new(1, -20, 0, 40)
        frame.Position = UDim2.new(0, 10, 0, 0)
        frame.ZIndex = 1002
        ApplyCorner(frame, 8)
        
        local labelText = Instance.new("TextLabel")
        labelText.Parent = frame
        labelText.BackgroundTransparency = 1
        labelText.Size = UDim2.new(0.6, 0, 1, 0)
        labelText.Position = UDim2.new(0, 10, 0, 0)
        labelText.Text = label
        labelText.TextColor3 = Colors.Text
        labelText.TextSize = 14
        labelText.TextXAlignment = Enum.TextXAlignment.Left
        labelText.Font = Enum.Font.Gotham
        labelText.ZIndex = 1003
        
        local switchBg = Instance.new("TextButton")
        switchBg.Parent = frame
        switchBg.BackgroundColor3 = default and Colors.Green or Colors.BG3
        switchBg.BorderSizePixel = 0
        switchBg.Size = UDim2.new(0, 50, 0, 25)
        switchBg.Position = UDim2.new(1, -60, 0.5, -12)
        switchBg.Text = ""
        switchBg.ZIndex = 1003
        switchBg.AutoButtonColor = false
        ApplyCorner(switchBg, 12)
        
        local circle = Instance.new("Frame")
        circle.Parent = switchBg
        circle.BackgroundColor3 = Colors.Text
        circle.BorderSizePixel = 0
        circle.Size = UDim2.new(0, 21, 0, 21)
        circle.Position = default and UDim2.new(1, -23, 0.5, -10) or UDim2.new(0, 2, 0.5, -10)
        circle.ZIndex = 1004
        ApplyCorner(circle, 10)
        
        local isOn = default
        
        switchBg.MouseButton1Click:Connect(function()
            isOn = not isOn
            if isOn then
                TweenService:Create(circle, TweenInfo.new(0.2), {Position = UDim2.new(1, -23, 0.5, -10)}):Play()
                TweenService:Create(switchBg, TweenInfo.new(0.2), {BackgroundColor3 = Colors.Green}):Play()
            else
                TweenService:Create(circle, TweenInfo.new(0.2), {Position = UDim2.new(0, 2, 0.5, -10)}):Play()
                TweenService:Create(switchBg, TweenInfo.new(0.2), {BackgroundColor3 = Colors.BG3}):Play()
            end
            callback(isOn)
        end)
        
        return {frame = frame, switchBg = switchBg, circle = circle}
    end
    
    local function CreateRainbowToggle(parent, callback, default, yPos)
        local toggle = CreateAndroidToggle(parent, "Радуга", callback, default)
        toggle.frame.Position = UDim2.new(0, 10, 0, yPos or 0)
        return toggle
    end
    
    local function UpdatePlayers()
        local container = containers[1].container
        for _, child in pairs(container:GetChildren()) do child:Destroy() end
        
        local y = 10
        local players = {}
        
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                local root = player.Character:FindFirstChild("HumanoidRootPart")
                local hum = player.Character:FindFirstChild("Humanoid")
                if root and hum then
                    table.insert(players, {
                        Name = player.Name,
                        Player = player,
                        Dist = (LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")) and (LocalPlayer.Character.HumanoidRootPart.Position - root.Position).Magnitude or 0,
                        Health = hum.Health,
                        MaxHealth = hum.MaxHealth,
                        Team = GetPlayerTeam(player),
                        TeamColor = GetTeamColor(player)
                    })
                end
            end
        end
        
        table.sort(players, function(a, b) return a.Dist < b.Dist end)
        
        if #players == 0 then
            local empty = Instance.new("TextLabel")
            empty.Parent = container
            empty.BackgroundTransparency = 1
            empty.Size = UDim2.new(1, -20, 0, 50)
            empty.Position = UDim2.new(0, 10, 0, y)
            empty.Text = "Нет игроков"
            empty.TextColor3 = Colors.TextDim
            empty.TextSize = 16
            empty.Font = Enum.Font.Gotham
            empty.ZIndex = 1002
            y = y + 55
        else
            for i, data in ipairs(players) do
                local btn = Instance.new("TextButton")
                btn.Parent = container
                btn.BackgroundColor3 = (i % 2 == 0) and Colors.BG2 or Colors.BG3
                btn.BackgroundTransparency = 0.3
                btn.BorderSizePixel = 1
                btn.BorderColor3 = data.TeamColor
                btn.Size = UDim2.new(1, -20, 0, 70)
                btn.Position = UDim2.new(0, 10, 0, y)
                btn.Text = ""
                btn.ZIndex = 1002
                ApplyCorner(btn, 8)
                
                btn.MouseButton1Click:Connect(function()
                    if data.Player and data.Player.Character then
                        local localRoot = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
                        local targetRoot = data.Player.Character:FindFirstChild("HumanoidRootPart")
                        if localRoot and targetRoot then
                            localRoot.CFrame = targetRoot.CFrame + Vector3.new(0, 3, 0)
                        end
                    end
                end)
                
                local nameLabel = Instance.new("TextLabel")
                nameLabel.Parent = btn
                nameLabel.BackgroundTransparency = 1
                nameLabel.Size = UDim2.new(0.35, 0, 0, 20)
                nameLabel.Position = UDim2.new(0, 10, 0, 5)
                nameLabel.Text = data.Name
                nameLabel.TextColor3 = Colors.Text
                nameLabel.TextSize = 13
                nameLabel.TextXAlignment = Enum.TextXAlignment.Left
                nameLabel.Font = Enum.Font.Gotham
                nameLabel.ZIndex = 1003
                
                local teamLabel = Instance.new("TextLabel")
                teamLabel.Parent = btn
                teamLabel.BackgroundTransparency = 1
                teamLabel.Size = UDim2.new(0.3, 0, 0, 20)
                teamLabel.Position = UDim2.new(0.35, 0, 0, 5)
                teamLabel.Text = data.Team
                teamLabel.TextColor3 = data.TeamColor
                teamLabel.TextSize = 11
                teamLabel.TextXAlignment = Enum.TextXAlignment.Left
                teamLabel.Font = Enum.Font.GothamBold
                teamLabel.ZIndex = 1003
                
                local distLabel = Instance.new("TextLabel")
                distLabel.Parent = btn
                distLabel.BackgroundTransparency = 1
                distLabel.Size = UDim2.new(0.3, 0, 0, 20)
                distLabel.Position = UDim2.new(0.65, 0, 0, 5)
                distLabel.Text = string.format("%.0fм", data.Dist)
                distLabel.TextColor3 = Colors.TextDim
                distLabel.TextSize = 11
                distLabel.TextXAlignment = Enum.TextXAlignment.Right
                distLabel.Font = Enum.Font.Gotham
                distLabel.ZIndex = 1003
                
                local hpBg = Instance.new("Frame")
                hpBg.Parent = btn
                hpBg.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
                hpBg.BorderSizePixel = 1
                hpBg.BorderColor3 = Colors.Border
                hpBg.Size = UDim2.new(0.6, 0, 0, 8)
                hpBg.Position = UDim2.new(0.2, 0, 0, 35)
                hpBg.ZIndex = 1003
                ApplyCorner(hpBg, 4)
                
                local hpPercent = math.clamp(data.Health / data.MaxHealth, 0, 1)
                local hpFill = Instance.new("Frame")
                hpFill.Parent = hpBg
                hpFill.BackgroundColor3 = hpPercent > 0.5 and Colors.Green or hpPercent > 0.25 and Colors.Yellow or Colors.Red
                hpFill.BorderSizePixel = 0
                hpFill.Size = UDim2.new(hpPercent, 0, 1, 0)
                hpFill.ZIndex = 1004
                ApplyCorner(hpFill, 4)
                
                local hpText = Instance.new("TextLabel")
                hpText.Parent = btn
                hpText.BackgroundTransparency = 1
                hpText.Size = UDim2.new(0.3, 0, 0, 20)
                hpText.Position = UDim2.new(0.65, 0, 0, 30)
                hpText.Text = string.format("%.0f/%.0f", data.Health, data.MaxHealth)
                hpText.TextColor3 = Colors.TextDim
                hpText.TextSize = 10
                hpText.TextXAlignment = Enum.TextXAlignment.Right
                hpText.Font = Enum.Font.Gotham
                hpText.ZIndex = 1003
                
                y = y + 75
            end
        end
        
        containers[1].frame.CanvasSize = UDim2.new(0, 0, 0, y + 10)
        container.Size = UDim2.new(1, 0, 0, y + 10)
    end
    
    local function CreatePlayerUI()
        local container = containers[2].container
        for _, child in pairs(container:GetChildren()) do child:Destroy() end
        
        local y = 10
        
        local flyTitle = Instance.new("TextLabel")
        flyTitle.Parent = container
        flyTitle.BackgroundTransparency = 1
        flyTitle.Size = UDim2.new(1, -20, 0, 25)
        flyTitle.Position = UDim2.new(0, 10, 0, y)
        flyTitle.Text = "ПОЛЕТ (Клавиша F)"
        flyTitle.TextColor3 = Colors.Accent2
        flyTitle.TextSize = 16
        flyTitle.Font = Enum.Font.GothamBold
        flyTitle.ZIndex = 1002
        y = y + 30
        
        local flyButton = MakeButton(container, FlyEnabled and "Выключить полет (F)" or "Включить полет (F)", function()
            if FlyEnabled then StopFly() else StartFly() end
            CreatePlayerUI()
        end, FlyEnabled and Colors.Red or Colors.Green)
        flyButton.Position = UDim2.new(0, 10, 0, y)
        y = y + 50
        
        local flySlider = MakeSlider(container, "Скорость полета", 10, 200, FlySpeed, function(val)
            FlySpeed = val
            _G.XenoFlySpeed = val
        end)
        flySlider.Position = UDim2.new(0, 0, 0, y)
        y = y + 70
        
        local modeLabel = Instance.new("TextLabel")
        modeLabel.Parent = container
        modeLabel.BackgroundTransparency = 1
        modeLabel.Size = UDim2.new(1, -20, 0, 25)
        modeLabel.Position = UDim2.new(0, 10, 0, y)
        modeLabel.Text = "Режим: " .. FlyMode
        modeLabel.TextColor3 = Colors.Accent2
        modeLabel.TextSize = 14
        modeLabel.Font = Enum.Font.Gotham
        modeLabel.ZIndex = 1002
        y = y + 30
        
        local modes = {"Свободный", "По курсору", "По взгляду"}
        for i, mode in ipairs(modes) do
            local btn = Instance.new("TextButton")
            btn.Parent = container
            btn.BackgroundColor3 = (FlyMode == mode) and Colors.Accent or Colors.BG3
            btn.BackgroundTransparency = (FlyMode == mode) and 0.1 or 0.2
            btn.BorderSizePixel = 1
            btn.BorderColor3 = Colors.Border
            btn.Size = UDim2.new(0.3, -8, 0, 32)
            btn.Position = UDim2.new((i - 1) * 0.333 + 0.015, 0, 0, y)
            btn.Text = mode
            btn.TextColor3 = (FlyMode == mode) and Colors.Text or Colors.TextDim
            btn.TextSize = 12
            btn.Font = Enum.Font.Gotham
            btn.ZIndex = 1002
            ApplyCorner(btn, 6)
            btn.MouseButton1Click:Connect(function()
                FlyMode = mode
                _G.XenoFlyMode = mode
                CreatePlayerUI()
            end)
        end
        y = y + 42
        
        local separator = Instance.new("Frame")
        separator.Parent = container
        separator.BackgroundColor3 = Colors.Border
        separator.BorderSizePixel = 0
        separator.Size = UDim2.new(1, -20, 0, 1)
        separator.Position = UDim2.new(0, 10, 0, y)
        separator.ZIndex = 1002
        y = y + 15
        
        local spinTitle = Instance.new("TextLabel")
        spinTitle.Parent = container
        spinTitle.BackgroundTransparency = 1
        spinTitle.Size = UDim2.new(1, -20, 0, 25)
        spinTitle.Position = UDim2.new(0, 10, 0, y)
        spinTitle.Text = "КРУТИЛКА"
        spinTitle.TextColor3 = Colors.Accent2
        spinTitle.TextSize = 16
        spinTitle.Font = Enum.Font.GothamBold
        spinTitle.ZIndex = 1002
        y = y + 30
        
        local spinToggle = CreateAndroidToggle(container, "Крутилка", function(enabled)
            if enabled then StartSpin() else StopSpin() end
        end, SpinEnabled)
        spinToggle.frame.Position = UDim2.new(0, 10, 0, y)
        y = y + 50
        
        local spinSlider = MakeSlider(container, "Скорость кручения", 1, 50, SpinSpeed, function(val)
            SpinSpeed = val
            _G.XenoSpinSpeed = val
            if SpinEnabled then StopSpin(); StartSpin() end
        end)
        spinSlider.Position = UDim2.new(0, 0, 0, y)
        y = y + 70
        
        local separator2 = Instance.new("Frame")
        separator2.Parent = container
        separator2.BackgroundColor3 = Colors.Border
        separator2.BorderSizePixel = 0
        separator2.Size = UDim2.new(1, -20, 0, 1)
        separator2.Position = UDim2.new(0, 10, 0, y)
        separator2.ZIndex = 1002
        y = y + 15
        
        local noClipTitle = Instance.new("TextLabel")
        noClipTitle.Parent = container
        noClipTitle.BackgroundTransparency = 1
        noClipTitle.Size = UDim2.new(1, -20, 0, 25)
        noClipTitle.Position = UDim2.new(0, 10, 0, y)
        noClipTitle.Text = "ПРОХОЖДЕНИЕ СКВОЗЬ СТЕНЫ"
        noClipTitle.TextColor3 = Colors.Accent2
        noClipTitle.TextSize = 16
        noClipTitle.Font = Enum.Font.GothamBold
        noClipTitle.ZIndex = 1002
        y = y + 30
        
        local noClipToggle = CreateAndroidToggle(container, "NoClip", function(enabled)
            if enabled then StartNoClip() else StopNoClip() end
        end, NoClipEnabled)
        noClipToggle.frame.Position = UDim2.new(0, 10, 0, y)
        y = y + 50
        
        local separator3 = Instance.new("Frame")
        separator3.Parent = container
        separator3.BackgroundColor3 = Colors.Border
        separator3.BorderSizePixel = 0
        separator3.Size = UDim2.new(1, -20, 0, 1)
        separator3.Position = UDim2.new(0, 10, 0, y)
        separator3.ZIndex = 1002
        y = y + 15
        
        local charTitle = Instance.new("TextLabel")
        charTitle.Parent = container
        charTitle.BackgroundTransparency = 1
        charTitle.Size = UDim2.new(1, -20, 0, 25)
        charTitle.Position = UDim2.new(0, 10, 0, y)
        charTitle.Text = "ХАРАКТЕРИСТИКИ"
        charTitle.TextColor3 = Colors.Accent2
        charTitle.TextSize = 16
        charTitle.Font = Enum.Font.GothamBold
        charTitle.ZIndex = 1002
        y = y + 30
        
        local walkSlider = MakeSlider(container, "Скорость ходьбы", 16, 100, WalkSpeed, function(val)
            WalkSpeed = val
            _G.XenoWalkSpeed = val
            ApplyCharacteristics()
        end)
        walkSlider.Position = UDim2.new(0, 0, 0, y)
        y = y + 70
        
        local jumpSlider = MakeSlider(container, "Сила прыжка", 50, 200, JumpPower, function(val)
            JumpPower = val
            _G.XenoJumpPower = val
            ApplyCharacteristics()
        end)
        jumpSlider.Position = UDim2.new(0, 0, 0, y)
        y = y + 70
        
        local gravitySlider = MakeSlider(container, "Гравитация", 0, 300, PlayerGravity, function(val)
            PlayerGravity = val
            _G.XenoPlayerGravity = val
            if not FlyEnabled then ApplyCharacteristics() end
        end)
        gravitySlider.Position = UDim2.new(0, 0, 0, y)
        y = y + 70
        
        containers[2].frame.CanvasSize = UDim2.new(0, 0, 0, y + 10)
        container.Size = UDim2.new(1, 0, 0, y + 10)
    end
    
    local function CreateESPUI()
        local container = containers[3].container
        for _, child in pairs(container:GetChildren()) do child:Destroy() end
        
        local y = 10
        
        local title = Instance.new("TextLabel")
        title.Parent = container
        title.BackgroundTransparency = 1
        title.Size = UDim2.new(1, -20, 0, 25)
        title.Position = UDim2.new(0, 10, 0, y)
        title.Text = "ESP НАСТРОЙКИ"
        title.TextColor3 = Colors.Accent2
        title.TextSize = 16
        title.Font = Enum.Font.GothamBold
        title.ZIndex = 1002
        y = y + 30
        
        local tracerToggle = CreateAndroidToggle(container, "Линии (Tracer)", function(enabled)
            TracerEnabled = enabled
            _G.XenoTracerEnabled = enabled
            UpdateESP()
        end, TracerEnabled)
        tracerToggle.frame.Position = UDim2.new(0, 10, 0, y)
        y = y + 50
        
        local boxToggle = CreateAndroidToggle(container, "Подсветка (Box)", function(enabled)
            BoxEnabled = enabled
            _G.XenoBoxEnabled = enabled
            UpdateESP()
        end, BoxEnabled)
        boxToggle.frame.Position = UDim2.new(0, 10, 0, y)
        y = y + 50
        
        local nametagsToggle = CreateAndroidToggle(container, "Имена (Nametags)", function(enabled)
            NametagsEnabled = enabled
            _G.XenoNametagsEnabled = enabled
            UpdateESP()
        end, NametagsEnabled)
        nametagsToggle.frame.Position = UDim2.new(0, 10, 0, y)
        y = y + 50
        
        local distanceSlider = MakeSlider(container, "Дальность ESP", 50, 2000, ESPDistance, function(val)
            ESPDistance = val
            _G.XenoESPDistance = val
            UpdateESP()
        end)
        distanceSlider.Position = UDim2.new(0, 0, 0, y)
        y = y + 70
        
        local teamColorToggle = CreateAndroidToggle(container, "Цвет команды", function(enabled)
            ESPTeamColor = enabled
            _G.XenoESPTeamColor = enabled
            if enabled then SetESPRainbow(false) end
            UpdateESP()
        end, ESPTeamColor)
        teamColorToggle.frame.Position = UDim2.new(0, 10, 0, y)
        y = y + 50
        
        local colorPalette = CreateColorPalette(container, "Цвет ESP:", ESPColor, function(color)
            ESPColor = color
            _G.XenoESPColor = color
            UpdateESP()
        end, y)
        y = y + 210
        
        local espRainbowToggle = CreateRainbowToggle(container, function(enabled)
            SetESPRainbow(enabled)
        end, ESPRainbow)
        espRainbowToggle.frame.Position = UDim2.new(0, 10, 0, y)
        y = y + 50
        
        containers[3].frame.CanvasSize = UDim2.new(0, 0, 0, y + 10)
        container.Size = UDim2.new(1, 0, 0, y + 10)
    end
    
    local function CreateMarksUI()
        local container = containers[4].container
        for _, child in pairs(container:GetChildren()) do child:Destroy() end
        
        local y = 10
        
        local title = Instance.new("TextLabel")
        title.Parent = container
        title.BackgroundTransparency = 1
        title.Size = UDim2.new(1, -20, 0, 25)
        title.Position = UDim2.new(0, 10, 0, y)
        title.Text = "МЕТКИ"
        title.TextColor3 = Colors.Accent2
        title.TextSize = 16
        title.Font = Enum.Font.GothamBold
        title.ZIndex = 1002
        y = y + 30
        
        local addCurrentBtn = MakeButton(container, "Добавить текущую позицию", function()
            local char = LocalPlayer.Character
            if char and char:FindFirstChild("HumanoidRootPart") then
                local pos = char.HumanoidRootPart.Position
                table.insert(Marks, {Name = "Метка " .. #Marks + 1, Position = pos})
                _G.XenoMarks = Marks
                CreateMarksUI()
            end
        end, Colors.Green)
        addCurrentBtn.Position = UDim2.new(0, 10, 0, y)
        y = y + 50
        
        if #Marks == 0 then
            local empty = Instance.new("TextLabel")
            empty.Parent = container
            empty.BackgroundTransparency = 1
            empty.Size = UDim2.new(1, -20, 0, 50)
            empty.Position = UDim2.new(0, 10, 0, y)
            empty.Text = "Нет меток"
            empty.TextColor3 = Colors.TextDim
            empty.TextSize = 16
            empty.Font = Enum.Font.Gotham
            empty.ZIndex = 1002
            y = y + 55
        else
            for i, mark in ipairs(Marks) do
                local markFrame = Instance.new("Frame")
                markFrame.Parent = container
                markFrame.BackgroundColor3 = Colors.BG2
                markFrame.BackgroundTransparency = 0.3
                markFrame.BorderSizePixel = 1
                markFrame.BorderColor3 = Colors.Border
                markFrame.Size = UDim2.new(1, -20, 0, 60)
                markFrame.Position = UDim2.new(0, 10, 0, y)
                markFrame.ZIndex = 1002
                ApplyCorner(markFrame, 8)
                
                local markName = Instance.new("TextLabel")
                markName.Parent = markFrame
                markName.BackgroundTransparency = 1
                markName.Size = UDim2.new(0.6, 0, 0, 20)
                markName.Position = UDim2.new(0, 10, 0, 5)
                markName.Text = mark.Name
                markName.TextColor3 = Colors.Text
                markName.TextSize = 13
                markName.TextXAlignment = Enum.TextXAlignment.Left
                markName.Font = Enum.Font.GothamBold
                markName.ZIndex = 1003
                
                local tpBtn = Instance.new("TextButton")
                tpBtn.Parent = markFrame
                tpBtn.BackgroundColor3 = Colors.Green
                tpBtn.BackgroundTransparency = 0.2
                tpBtn.BorderSizePixel = 1
                tpBtn.BorderColor3 = Colors.Border
                tpBtn.Size = UDim2.new(0.2, 0, 0, 30)
                tpBtn.Position = UDim2.new(0.75, 0, 0, 15)
                tpBtn.Text = "ТП"
                tpBtn.TextColor3 = Colors.Text
                tpBtn.TextSize = 12
                tpBtn.Font = Enum.Font.GothamBold
                tpBtn.ZIndex = 1003
                ApplyCorner(tpBtn, 6)
                tpBtn.MouseButton1Click:Connect(function()
                    local char = LocalPlayer.Character
                    if char and char:FindFirstChild("HumanoidRootPart") then
                        char.HumanoidRootPart.CFrame = CFrame.new(mark.Position)
                    end
                end)
                
                local delBtn = Instance.new("TextButton")
                delBtn.Parent = markFrame
                delBtn.BackgroundColor3 = Colors.Red
                delBtn.BackgroundTransparency = 0.2
                delBtn.BorderSizePixel = 1
                delBtn.BorderColor3 = Colors.Border
                delBtn.Size = UDim2.new(0.1, 0, 0, 30)
                delBtn.Position = UDim2.new(0.88, 0, 0, 15)
                delBtn.Text = "✕"
                delBtn.TextColor3 = Colors.Text
                delBtn.TextSize = 12
                delBtn.Font = Enum.Font.GothamBold
                delBtn.ZIndex = 1003
                ApplyCorner(delBtn, 6)
                delBtn.MouseButton1Click:Connect(function()
                    table.remove(Marks, i)
                    _G.XenoMarks = Marks
                    CreateMarksUI()
                end)
                
                y = y + 65
            end
        end
        
        containers[4].frame.CanvasSize = UDim2.new(0, 0, 0, y + 10)
        container.Size = UDim2.new(1, 0, 0, y + 10)
    end
    
    local function CreateCustomUI()
        local container = containers[5].container
        for _, child in pairs(container:GetChildren()) do child:Destroy() end
        
        local y = 10
        
        local title = Instance.new("TextLabel")
        title.Parent = container
        title.BackgroundTransparency = 1
        title.Size = UDim2.new(1, -20, 0, 30)
        title.Position = UDim2.new(0, 10, 0, y)
        title.Text = "КАСТОМИЗАЦИЯ"
        title.TextColor3 = Colors.Accent2
        title.TextSize = 16
        title.Font = Enum.Font.GothamBold
        title.ZIndex = 1002
        y = y + 35
        
        local bgPalette = CreateColorPalette(container, "Цвет фона:", MenuColor, function(color)
            MenuColor = color
            _G.XenoMenuColor = color
            MainFrame.BackgroundColor3 = color
        end, y)
        y = y + 210
        
        local bgRainbowToggle = CreateRainbowToggle(container, function(enabled)
            BGRainbow = enabled
            _G.XenoBGRainbow = enabled
            if BGRainbowConn then BGRainbowConn:Disconnect() BGRainbowConn = nil end
            if enabled then
                local hue = 0
                BGRainbowConn = RunService.RenderStepped:Connect(function(dt)
                    hue = (hue + dt * 0.3) % 1
                    MenuColor = HSVToColor(hue, 1, 1)
                    _G.XenoMenuColor = MenuColor
                    MainFrame.BackgroundColor3 = MenuColor
                end)
            end
        end, BGRainbow)
        bgRainbowToggle.frame.Position = UDim2.new(0, 10, 0, y)
        y = y + 50
        
        local accentPalette = CreateColorPalette(container, "Цвет акцента:", AccentColor, function(color)
            AccentColor = color
            _G.XenoAccentColor = color
            Colors.Accent = color
            Header.BackgroundColor3 = color
        end, y)
        y = y + 210
        
        local accentRainbowToggle = CreateRainbowToggle(container, function(enabled)
            AccentRainbow = enabled
            _G.XenoAccentRainbow = enabled
            if AccentRainbowConn then AccentRainbowConn:Disconnect() AccentRainbowConn = nil end
            if enabled then
                local hue = 0
                AccentRainbowConn = RunService.RenderStepped:Connect(function(dt)
                    hue = (hue + dt * 0.3) % 1
                    AccentColor = HSVToColor(hue, 1, 1)
                    _G.XenoAccentColor = AccentColor
                    Colors.Accent = AccentColor
                    Header.BackgroundColor3 = AccentColor
                end)
            end
        end, AccentRainbow)
        accentRainbowToggle.frame.Position = UDim2.new(0, 10, 0, y)
        y = y + 50
        
        local transSlider = MakeSlider(container, "Прозрачность", 0, 0.5, MenuTransparency, function(val)
            MenuTransparency = val
            _G.XenoMenuTransparency = val
            MainFrame.BackgroundTransparency = val
        end)
        transSlider.Position = UDim2.new(0, 0, 0, y)
        y = y + 70
        
        local refreshBtn = MakeButton(container, "Обновить галерею", function()
            GalleryImages = {}
            local rng = Random.new()
            for i = 1, 30 do
                local id = rng:NextInteger(1000000, 99999999)
                table.insert(GalleryImages, {Name = "Картинка #" .. id, ID = "rbxassetid://" .. id})
            end
            CreateCustomUI()
        end, Colors.Green)
        refreshBtn.Position = UDim2.new(0, 10, 0, y)
        y = y + 50
        
        local galleryTitle = Instance.new("TextLabel")
        galleryTitle.Parent = container
        galleryTitle.BackgroundTransparency = 1
        galleryTitle.Size = UDim2.new(1, -20, 0, 25)
        galleryTitle.Position = UDim2.new(0, 10, 0, y)
        galleryTitle.Text = "ГАЛЕРЕЯ"
        galleryTitle.TextColor3 = Colors.Accent2
        galleryTitle.TextSize = 14
        galleryTitle.Font = Enum.Font.GothamBold
        galleryTitle.ZIndex = 1002
        y = y + 30
        
        if #GalleryImages == 0 then
            local rng = Random.new()
            for i = 1, 30 do
                local id = rng:NextInteger(1000000, 99999999)
                table.insert(GalleryImages, {Name = "Картинка #" .. id, ID = "rbxassetid://" .. id})
            end
        end
        
        local columns = 3
        local imageWidth = 120
        local imageHeight = 80
        local spacing = 5
        
        for i, img in ipairs(GalleryImages) do
            local col = (i - 1) % columns
            local row = math.floor((i - 1) / columns)
            
            local imgFrame = Instance.new("Frame")
            imgFrame.Name = "GalleryItem"
            imgFrame.Parent = container
            imgFrame.BackgroundColor3 = Colors.BG2
            imgFrame.BorderSizePixel = 1
            imgFrame.BorderColor3 = Colors.Border
            imgFrame.Size = UDim2.new(0, imageWidth, 0, imageHeight + 20)
            imgFrame.Position = UDim2.new(0, 10 + col * (imageWidth + spacing), 0, y + row * (imageHeight + 25))
            imgFrame.ZIndex = 1002
            ApplyCorner(imgFrame, 6)
            
            local imgButton = Instance.new("ImageButton")
            imgButton.Parent = imgFrame
            imgButton.BackgroundColor3 = Colors.BG3
            imgButton.BorderSizePixel = 0
            imgButton.Size = UDim2.new(1, -2, 0, imageHeight)
            imgButton.Position = UDim2.new(0, 1, 0, 1)
            imgButton.Image = img.ID
            imgButton.ScaleType = Enum.ScaleType.Crop
            imgButton.ZIndex = 1003
            ApplyCorner(imgButton, 5)
            imgButton.AutoButtonColor = false
            
            local nameLabel = Instance.new("TextLabel")
            nameLabel.Parent = imgFrame
            nameLabel.BackgroundTransparency = 1
            nameLabel.Size = UDim2.new(1, 0, 0, 15)
            nameLabel.Position = UDim2.new(0, 3, 0, imageHeight + 3)
            nameLabel.Text = img.Name
            nameLabel.TextColor3 = Colors.Text
            nameLabel.TextSize = 8
            nameLabel.Font = Enum.Font.Gotham
            nameLabel.ZIndex = 1003
            
            imgButton.MouseButton1Click:Connect(function()
                for _, child in pairs(MainFrame:GetChildren()) do
                    if child:IsA("ImageLabel") then child:Destroy() end
                end
                local image = Instance.new("ImageLabel")
                image.Parent = MainFrame
                image.BackgroundTransparency = 1
                image.Size = UDim2.new(1, 0, 1, -40)
                image.Position = UDim2.new(0, 0, 0, 40)
                image.Image = img.ID
                image.ZIndex = 998
                image.ScaleType = Enum.ScaleType.Crop
                ApplyCorner(image, 12)
                SavedBackgroundID = img.ID
                _G.XenoBackgroundID = img.ID
            end)
        end
        
        local pagesY = y + math.ceil(#GalleryImages / columns) * (imageHeight + 25) + 10
        
        local removeBtn = MakeButton(container, "Убрать фон", function()
            SavedBackgroundID = nil
            _G.XenoBackgroundID = nil
            for _, child in pairs(MainFrame:GetChildren()) do
                if child:IsA("ImageLabel") then child:Destroy() end
            end
        end, Colors.Red)
        removeBtn.Position = UDim2.new(0, 10, 0, pagesY)
        pagesY = pagesY + 50
        
        local credits = {
            "Создатель: babykity",
            "Телеграм: @minexcfu",
            "Дискорд: 777miroslav777",
            "Помощник: kakaroldod"
        }
        
        local creditY = pagesY
        for _, credit in ipairs(credits) do
            local label = Instance.new("TextLabel")
            label.Parent = container
            label.BackgroundTransparency = 1
            label.Size = UDim2.new(1, -20, 0, 20)
            label.Position = UDim2.new(0, 10, 0, creditY)
            label.Text = credit
            label.TextColor3 = Colors.TextDim
            label.TextSize = 12
            label.Font = Enum.Font.Gotham
            label.ZIndex = 1002
            creditY = creditY + 25
        end
        
        containers[5].frame.CanvasSize = UDim2.new(0, 0, 0, creditY + 50)
        container.Size = UDim2.new(1, 0, 0, creditY + 50)
    end
    
    local function SwitchTab(tabIndex)
        for j = 1, 5 do
            local isActive = (j == tabIndex)
            local targetColor = isActive and Colors.Accent or Colors.BG3
            local targetTransparency = isActive and 0.1 or 0.2
            local targetTextColor = isActive and Colors.Text or Colors.TextDim
            TweenService:Create(tabs[j], TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                BackgroundColor3 = targetColor,
                BackgroundTransparency = targetTransparency,
                TextColor3 = targetTextColor
            }):Play()
            if isActive then
                containers[j].frame.Visible = true
                containers[j].frame.Position = UDim2.new(0, 140, 0, 80)
                TweenService:Create(containers[j].frame, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                    Position = UDim2.new(0, 120, 0, 80)
                }):Play()
            else
                containers[j].frame.Visible = false
            end
        end
        if tabIndex == 1 then UpdatePlayers()
        elseif tabIndex == 2 then CreatePlayerUI()
        elseif tabIndex == 3 then CreateESPUI()
        elseif tabIndex == 4 then CreateMarksUI()
        elseif tabIndex == 5 then CreateCustomUI() end
    end
    
    for i = 1, 5 do
        tabs[i].MouseButton1Click:Connect(function()
            SwitchTab(i)
        end)
    end
    
    UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.KeyCode == Enum.KeyCode.F and not gameProcessed then
            if FlyEnabled then StopFly() else StartFly() end
            if containers[2].frame.Visible then CreatePlayerUI() end
        end
    end)
    
    UpdatePlayers()
    CreatePlayerUI()
    CreateESPUI()
    CreateMarksUI()
    CreateCustomUI()
end

CreateMenu()
print("XENO MENU успешно запущен!")
